<h1><%=@user.username%>'s Conversations.</h1>
<br>
<% @conversations.each do |conversation| %>
  <div class="convo-box">
    <%= conversation.name %>
    <%= conversation.messages.last %>
    <%= conversation.edited_at%>
  </div>
<% end %>
